import Layout from '../components/layout/Layout';
import MenuGrid from '../components/menu/MenuGrid';

const Menu = () => {
  return (
    <Layout>
      <MenuGrid />
    </Layout>
  );
};

export default Menu;